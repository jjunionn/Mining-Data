import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score, classification_report
from sklearn.decomposition import PCA
from sklearn.ensemble import RandomForestClassifier
from google.colab import files
from sklearn.metrics import classification_report

# Upload file manual dari komputer
uploaded = files.upload()

# ==== LOAD DATA ====
df = pd.read_csv("diabetes.csv", sep=";")
print("Data asli:")
print(df.head())

# Fitur (X) pakai semua kolom numerik
X = df[["Umur", "BMI", "Glukosa", "Tekanan_Darah", "Aktivitas"]]
y = df["Diabetes"]

# Split data 70:30
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.3, random_state=42
)

# ==== PILIH ALGORITMA ====
model = RandomForestClassifier(n_estimators=100, random_state=42)

# ==== TRAINING ====
model.fit(X_train, y_train)

# ==== PREDIKSI ====
y_pred = model.predict(X_test)

# ==== EVALUASI ====
print("\n=== Evaluasi Model ===")
print("Accuracy:", accuracy_score(y_test, y_pred))
print(classification_report(y_test, y_pred, zero_division=0))

# ==== VISUALISASI ====
# Karena fitur lebih dari 2, boundary 2D tidak bisa divisualkan langsung.
# Jadi kita buat visualisasi sederhana perbandingan prediksi vs real
pca = PCA(n_components=2)
X_pca = pca.fit_transform(X)

# Konversi label 'Ya'/'Tidak' ke 1/0 untuk visualisasi
y_numeric = (y == "Ya").astype(int)

plt.figure(figsize=(8,6))
plt.scatter(X_pca[:,0], X_pca[:,1], c=y_numeric, cmap=plt.cm.Paired, edgecolor="k", s=80)
plt.xlabel("PC1")
plt.ylabel("PC2")
plt.title(f"Visualisasi PCA - {type(model).__name__}")
plt.colorbar(label="Diabetes (1=Ya, 0=Tidak)")
plt.show()
