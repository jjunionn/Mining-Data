from sklearn.preprocessing import LabelEncoder

# ==== LOAD DATA ====
df = pd.read_csv("loan.csv", sep=",")

# Encode kolom kategorikal
le = LabelEncoder()
df["Pekerjaan"] = le.fit_transform(df["Pekerjaan"])

print(df.head())

# Fitur (X) pakai semua kolom numerik (setelah encoding)
X = df[["Penghasilan", "Pekerjaan", "Umur"]]
y = df["Pinjaman_Disetuju"]



from sklearn.preprocessing import LabelEncoder

le = LabelEncoder()
df["Pekerjaan"] = le.fit_transform(df["Pekerjaan"])
df["Nama"] = le.fit_transform(df["Nama"])
df["Alamat"] = le.fit_transform(df["Alamat"])


from sklearn.preprocessing import LabelEncoder

le = LabelEncoder()
string_cols = ["Pekerjaan", "Nama", "Alamat"]
df[string_cols] = df[string_cols].apply(lambda col: le.fit_transform(col))
